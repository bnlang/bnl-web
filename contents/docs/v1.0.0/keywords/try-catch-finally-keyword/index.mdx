---
title: try / catch / finally
description: Handling thrown values in Bnlang with try / catch / finally.
bnTitle: চেষ্টা / ধরুন / অবশেষে
bnDescription: Bnlang-এ try / catch / finally দিয়ে throw করা মান হ্যান্ডল করা।
---

# <I18nEnglish>`try` / `catch` / `finally`</I18nEnglish>
# <I18nBangla>`চেষ্টা` / `ধরুন` / `অবশেষে`</I18nBangla>


<I18nEnglish>
A `try` block contains code that may throw. A trailing `catch (<name>) { ... }` receives the thrown value. An optional `finally { ... }` block runs regardless of whether the try succeeded or threw — useful for cleanup. The Bangla forms `চেষ্টা` / `ধরুন` / `অবশেষে` are equivalent.

## Syntax

```bnl
try {
    // code that may throw
} catch (<name>) {
    // runs only when the try block threw; <name> binds the thrown value
} finally {
    // runs in both cases (optional)
}
```

`catch` is required; `finally` is optional.

## Examples

```bnl
function divide(a, b) {
    if (b == 0) { throw "divide by zero"; }
    return a / b;
}

try {
    var result = divide(10, 0);
    print(result);
} catch (e) {
    print("caught:", e);
} finally {
    print("done");
}

// Output:
// caught: divide by zero
// done
```

## Cleanup with finally

`finally` is the right place for resource cleanup so it always runs:

```bnl
import "io" as io;

var f = io.open_read("notes.txt");

try {
    f.read(64, function (err, chunk) { /* ... */ });
} catch (e) {
    print("read failed:", e);
} finally {
    f.close();
}
```

## Catching specific shapes

The catch parameter is just a plain value — inspect it to branch:

```bnl
try {
    do_work();
} catch (e) {
    if (type(e) == "map" and e.code != null) {
        print("error code:", e.code, "-", e.message);
    } else {
        print("unexpected error:", e);
    }
}
```

## Rethrowing

You can re-throw the caught value (or a new one) from inside `catch`:

```bnl
try {
    risky();
} catch (e) {
    print("logging error:", e);
    throw e;   // bubble up
}
```

## Bangla form

```bnl
চেষ্টা {
    var result = divide(10, 0);
} ধরুন (e) {
    print("caught:", e);
} অবশেষে {
    print("done");
}
```

## See also
- [`throw`](/en/docs/v1.0.0/keywords/throw-keyword) — raise an error.
</I18nEnglish>

<I18nBangla>
একটি `try` ব্লকে এমন কোড থাকে যা throw করতে পারে। trailing `catch (<name>) { ... }` throw করা মান পায়। ঐচ্ছিক `finally { ... }` ব্লক চলে — try সফল হোক বা throw করুক — cleanup-এর জন্য উপযোগী। বাংলা রূপ `চেষ্টা` / `ধরুন` / `অবশেষে` সমার্থক।

## সিনট্যাক্স

```bnl
try {
    // throw করতে পারে এমন কোড
} catch (<name>) {
    // try থ্রো করলে শুধু চলে; <name> থ্রো করা মান বাইন্ড করে
} finally {
    // উভয় ক্ষেত্রে চলে (ঐচ্ছিক)
}
```

`catch` আবশ্যক; `finally` ঐচ্ছিক।

## উদাহরণ

```bnl
function divide(a, b) {
    if (b == 0) { throw "divide by zero"; }
    return a / b;
}

try {
    var result = divide(10, 0);
    print(result);
} catch (e) {
    print("caught:", e);
} finally {
    print("done");
}

// আউটপুট:
// caught: divide by zero
// done
```

## finally দিয়ে cleanup

`finally` রিসোর্স cleanup-এর সঠিক জায়গা যাতে এটি সবসময় চলে:

```bnl
import "io" as io;

var f = io.open_read("notes.txt");

try {
    f.read(64, function (err, chunk) { /* ... */ });
} catch (e) {
    print("read failed:", e);
} finally {
    f.close();
}
```

## নির্দিষ্ট shape catch

catch parameter একটি সাধারণ মান — branch করতে inspect করুন:

```bnl
try {
    do_work();
} catch (e) {
    if (type(e) == "map" and e.code != null) {
        print("error code:", e.code, "-", e.message);
    } else {
        print("unexpected error:", e);
    }
}
```

## Rethrowing

`catch`-এর ভেতর থেকে catch করা মান (অথবা নতুন) re-throw করতে পারেন:

```bnl
try {
    risky();
} catch (e) {
    print("logging error:", e);
    throw e;   // bubble up
}
```

## বাংলা রূপ

```bnl
চেষ্টা {
    var result = divide(10, 0);
} ধরুন (e) {
    print("caught:", e);
} অবশেষে {
    print("done");
}
```

## আরও দেখুন
- [`throw`](/bn/docs/v1.0.0/keywords/throw-keyword) — একটি এরর রেইজ।
</I18nBangla>
