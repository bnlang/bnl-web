---
title: Overview of Blocking vs Non-Blocking
description: A detailed guide to blocking and non-blocking operations in Bnlang with examples in English, Bangla, and Banglish.
bnTitle: ব্লকিং বনাম নন-ব্লকিং এর ওভারভিউ
bnDescription: Bnlang এ ব্লকিং এবং নন-ব্লকিং অপারেশন এর ব্যাখ্যা ও উদাহরণ।
banglishTitle: Overview of Blocking vs Non-Blocking
banglishDescription: Bnlang e blocking vs non-blocking operation er bistarito udahoron English, Bangla ar Banglish e.
---

# <I18nEnglish>Overview of Blocking vs Non-Blocking</I18nEnglish>
# <I18nBangla>ব্লকিং বনাম নন-ব্লকিং এর ওভারভিউ</I18nBangla>
# <I18nBanglish>Overview of Blocking vs Non-Blocking</I18nBanglish>

<I18nEnglish>
In **Bnlang**, operations can be **blocking** or **non-blocking**.  
- **Blocking** means the program waits until the operation finishes before moving on.  
- **Non-blocking** means the program continues running other tasks while the operation works in the background.  

Understanding this difference is key to writing efficient and responsive programs.
</I18nEnglish>

<I18nBangla>
**Bnlang** এ দুটি ধরণের অপারেশন আছে: **ব্লকিং** এবং **নন-ব্লকিং**।  
- **ব্লকিং** মানে প্রোগ্রাম অপেক্ষা করবে যতক্ষণ না কাজ শেষ হয়।  
- **নন-ব্লকিং** মানে প্রোগ্রাম কাজ চলাকালীন অন্য কোড চালিয়ে যাবে।  

এটি বোঝা জরুরি যাতে দক্ষ এবং রেসপন্সিভ প্রোগ্রাম লেখা যায়।
</I18nBangla>

<I18nBanglish>
**Bnlang** e operation duto dhoroner hote pare: **blocking** ar **non-blocking**.  
- **Blocking** mane holo program wait kore kaj sesh howar jonno.  
- **Non-blocking** mane holo program onno kaj cholate thake, kaj background e cholte thake.  

Eta bujhle efficient ar responsive program banano jay.
</I18nBanglish>

---

## Blocking Example

<Tabs defaultValue="bangla" className="w-full">
  <TabsList>
    <TabsTrigger value="bangla">Bangla</TabsTrigger>
    <TabsTrigger value="banglish">Banglish</TabsTrigger>
    <TabsTrigger value="english">English</TabsTrigger>
  </TabsList>

  <TabsContent value="english">

```js
// Simulated blocking function
function sleep(ms) {
  const end = Date.now() + ms;
  while (Date.now() < end) {}
}

console.log("Start");
sleep(2000); // Blocks for 2 seconds
console.log("End");
```
  </TabsContent>

  <TabsContent value="bangla">

```bnl
// ব্লকিং ফাংশনের উদাহরণ
function ঘুমাও(ms) {
  const শেষ = Date.now() + ms;
  যতক্ষণ (Date.now() < শেষ) {}
}

কনসোল.লগ("শুরু");
ঘুমাও(2000); // ২ সেকেন্ড ব্লক করে
কনসোল.লগ("শেষ");
```
  </TabsContent>

  <TabsContent value="banglish">

```bnl
// Blocking function er udahoron
function ghumao(ms) {
  const shesh = Date.now() + ms;
  jotokkhon (Date.now() < shesh) {}
}

console.log("Shuru");
ghumao(2000); // 2 second block kore
console.log("Shesh");
```
  </TabsContent>
</Tabs>

---

## Non-Blocking Example

<Tabs defaultValue="bangla" className="w-full">
  <TabsList>
    <TabsTrigger value="bangla">Bangla</TabsTrigger>
    <TabsTrigger value="banglish">Banglish</TabsTrigger>
    <TabsTrigger value="english">English</TabsTrigger>
  </TabsList>

  <TabsContent value="english">

```js
console.log("Start");

setTimeout(() => {
  console.log("Inside timeout");
}, 2000);

console.log("End"); // Printed immediately
```
  </TabsContent>

  <TabsContent value="bangla">

```bnl
কনসোল.লগ("শুরু");

সময়সীমা_নির্ধারণ(() => {
  কনসোল.লগ("টাইমআউটের ভেতরে");
}, 2000);

কনসোল.লগ("শেষ"); // সাথে সাথে প্রিন্ট হয়
```
  </TabsContent>

  <TabsContent value="banglish">

```bnl
console.log("Shuru");

somoysimaNirdharon(() => {
  console.log("Timeout er vitore");
}, 2000);

console.log("Shesh"); // Shathe shathe print hoy
```
  </TabsContent>
</Tabs>

---

## Why Non-Blocking is Better

<I18nEnglish>
- Keeps programs **responsive**.  
- Multiple tasks can progress together.  
- Better for I/O operations like file read, network requests, and database queries.
</I18nEnglish>

<I18nBangla>
- প্রোগ্রামকে **রেসপন্সিভ** রাখে।  
- একাধিক কাজ একসাথে চলতে পারে।  
- ফাইল পড়া, নেটওয়ার্ক রিকোয়েস্ট, এবং ডাটাবেস কোয়েরির জন্য উপযোগী।
</I18nBangla>

<I18nBanglish>
- Program ke **responsive** rakhe.  
- Ekadik kaj ekshathe cholte pare.  
- File read, network request, database query er moto I/O operation er jonno valo.
</I18nBanglish>

---

## Best Practices

<I18nEnglish>
- Avoid blocking operations in the main thread.  
- Use asynchronous APIs for I/O tasks.  
- Break heavy computations into smaller chunks or move them to workers.
</I18nEnglish>

<I18nBangla>
- মূল থ্রেডে ব্লকিং অপারেশন ব্যবহার করবেন না।  
- I/O কাজের জন্য অ্যাসিঙ্ক্রোনাস এপিআই ব্যবহার করুন।  
- ভারী কম্পিউটেশন ছোট অংশে ভাগ করুন অথবা আলাদা ওয়ার্কারে চালান।
</I18nBangla>

<I18nBanglish>
- Main thread e blocking operation avoid korun.  
- I/O kaj er jonno async API use korun.  
- Heavy computation ke chhoto vag e vag korun ba worker e chalun.
</I18nBanglish>
