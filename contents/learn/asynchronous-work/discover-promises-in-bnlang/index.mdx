---
title: Discover Promises in Bnlang
description: Learn how Promises work in Bnlang with examples in English, Bangla, and Banglish.
bnTitle: Bnlang এ প্রতিশ্রুতি আবিষ্কার
bnDescription: প্রতিশ্রুতি কীভাবে কাজ করে এবং উদাহরণ সহ Bnlang এ ব্যাখ্যা।
---

# <I18nEnglish>Discover Promises in Bnlang</I18nEnglish>
# <I18nBangla>Bnlang এ প্রতিশ্রুতি আবিষ্কার</I18nBangla>

<I18nEnglish>
A **Promise** in **Bnlang** represents a value that may be available now, later, or never.  
It is a structured way to handle asynchronous operations without falling into callback nesting.  
A promise has three states: **pending**, **fulfilled**, and **rejected**.
</I18nEnglish>

<I18nBangla>
**Bnlang** এ **প্রতিশ্রুতি** হলো এমন একটি অবজেক্ট যা কোনো মান এখন, পরে, অথবা কখনো প্রদান করতে পারে।  
এটি কলব্যাকের জটিলতা এড়িয়ে অ্যাসিঙ্ক্রোনাস কাজকে সহজভাবে হ্যান্ডল করতে সাহায্য করে।  
একটি প্রতিশ্রুতিের তিনটি অবস্থা আছে: **pending**, **fulfilled**, এবং **rejected**।
</I18nBangla>

---

## Creating a Promise

<Tabs defaultValue="english" className="w-full">
  <TabsList>
    <TabsTrigger value="english">English</TabsTrigger>
    <TabsTrigger value="bangla">Bangla</TabsTrigger>
    
  </TabsList>

  <TabsContent value="english">

```js
const myPromise = new Promise((resolve, reject) => {
  const success = true;
  if (success) {
    resolve("Operation successful!");
  } else {
    reject("Operation failed!");
  }
});

myPromise.then((msg) => console.log(msg))
         .catch((err) => console.error(err));
```
  </TabsContent>

  <TabsContent value="bangla">

```bnl
const আমার_প্রতিশ্রুতি = নতুন প্রতিশ্রুতি((রিজলভ, রিজেক্ট) => {
  const সফল = true;
  যদি (সফল) {
    রিজলভ("অপারেশন সফল!");
  } নাহলে {
    রিজেক্ট("অপারেশন ব্যর্থ!");
  }
});

আমার_প্রতিশ্রুতি.তারপর((বার্তা) => কনসোল.লগ(বার্তা))
        .ধরুন((এরর) => কনসোল.ত্রুটি(এরর));
```
  </TabsContent>

  
</Tabs>

---

## Chaining Promises

<I18nEnglish>
Promises can be chained using `.then()` to handle sequences of asynchronous tasks.
</I18nEnglish>

<I18nBangla>
একাধিক অ্যাসিঙ্ক্রোনাস টাস্ক সিকোয়েন্স আকারে চালাতে `.then()` ব্যবহার করা যায়।
</I18nBangla>

<Tabs defaultValue="english" className="w-full">
  <TabsList>
    <TabsTrigger value="english">English</TabsTrigger>
    <TabsTrigger value="bangla">Bangla</TabsTrigger>
  </TabsList>

  <TabsContent value="english">

```js
new Promise((resolve) => resolve(2))
  .then((n) => n * 2)
  .then((n) => n * 3)
  .then((result) => console.log(result)); // 12
```
  </TabsContent>

  <TabsContent value="bangla">

```bnl
নতুন প্রতিশ্রুতি((রিজলভ) => রিজলভ(2))
  .তারপর((n) => n * 2)
  .তারপর((n) => n * 3)
  .তারপর((ফলাফল) => কনসোল.লগ(ফলাফল)); // 12
```
  </TabsContent>

  
</Tabs>

---

## Promise.all

<I18nEnglish>
`Promise.all` runs multiple promises in parallel and resolves when all are fulfilled.  
If any fail, the whole promise rejects.
</I18nEnglish>

<I18nBangla>
`Promise.all` একাধিক প্রতিশ্রুতি একসাথে চালায় এবং সবগুলো পূর্ণ হলে রিজলভ হয়।  
একটি ব্যর্থ হলে পুরো প্রতিশ্রুতি রিজেক্ট হয়।
</I18nBangla>

<Tabs defaultValue="english" className="w-full">
  <TabsList>
    <TabsTrigger value="english">English</TabsTrigger>
    <TabsTrigger value="bangla">Bangla</TabsTrigger>
    
  </TabsList>

  <TabsContent value="english">

```js
Promise.all([
  Promise.resolve(1),
  Promise.resolve(2),
  Promise.resolve(3),
]).then((values) => console.log(values)); // [1,2,3]
```
  </TabsContent>

  <TabsContent value="bangla">

```bnl
প্রতিশ্রুতি.সব([
  প্রতিশ্রুতি.রিজলভ(1),
  প্রতিশ্রুতি.রিজলভ(2),
  প্রতিশ্রুতি.রিজলভ(3),
]).তারপর((মান) => কনসোল.লগ(মান)); // [1,2,3]
```
  </TabsContent>

  
</Tabs>

---

## Best Practices

<I18nEnglish>
- Always handle errors with `.catch()` to avoid unhandled rejections.  
- Use `Promise.all` or `Promise.race` for running tasks together.  
- Keep promises short and focused for readability.
</I18nEnglish>

<I18nBangla>
- আনহ্যান্ডলড রিজেকশন এড়াতে `.catch()` দিয়ে এরর হ্যান্ডল করুন।  
- একাধিক কাজ চালাতে `Promise.all` বা `Promise.race` ব্যবহার করুন।  
- পড়ার সহজতার জন্য প্রতিশ্রুতি ছোট ও নির্দিষ্ট রাখুন।
</I18nBangla>
