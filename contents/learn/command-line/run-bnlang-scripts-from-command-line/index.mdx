---
title: Run Bnlang Scripts from Command Line
description: How to run Bnlang scripts from the terminal — files, inline code, and passing arguments.
bnTitle: কমান্ড লাইন থেকে Bnlang স্ক্রিপ্ট চালানো
bnDescription: টার্মিনাল থেকে Bnlang স্ক্রিপ্ট চালানো — ফাইল, ইনলাইন কোড, এবং আর্গুমেন্ট পাঠানো।
---

# <I18nEnglish>Run Bnlang Scripts from Command Line</I18nEnglish>
# <I18nBangla>কমান্ড লাইন থেকে Bnlang স্ক্রিপ্ট চালানো</I18nBangla>


<I18nEnglish>
The `bnl` CLI is the single entry point to the runtime. It can run a script file, evaluate inline code with `-e`, or drop into a REPL when called with no arguments.
</I18nEnglish>

<I18nBangla>
`bnl` CLI রানটাইমের একমাত্র এন্ট্রি পয়েন্ট। এটি একটি স্ক্রিপ্ট ফাইল চালাতে পারে, `-e` দিয়ে ইনলাইন কোড ইভ্যালুয়েট করতে পারে, অথবা কোনো আর্গুমেন্ট ছাড়া কল করলে REPL-এ ঢোকে।
</I18nBangla>



---

## Running a Script File

```sh
# Run a script
bnl hello.bnl
```

<Tabs defaultValue="english" className="w-full">
  <TabsList>
    <TabsTrigger value="english">English</TabsTrigger>
    <TabsTrigger value="bangla">Bangla</TabsTrigger>
  </TabsList>

  <TabsContent value="english">

```bnl
// file: hello.bnl
print("Hello, World!");
```
  </TabsContent>

  <TabsContent value="bangla">

```bnl
// ফাইল: hello.bnl
লিখুন("হ্যালো, বিশ্ব!");
```
  </TabsContent>
</Tabs>

---

## Inline Code with -e

```sh
bnl -e 'print(1 + 2);'
# 3
```

---

## Starting the REPL

<I18nEnglish>
Running `bnl` with no arguments opens an interactive prompt. Useful for poking at the language or trying out stdlib calls.
</I18nEnglish>

<I18nBangla>
`bnl` কোনো আর্গুমেন্ট ছাড়া চালালে একটি ইন্টারেক্টিভ প্রম্পট খুলে। ভাষা যাচাই বা stdlib কল ট্রাই করতে কাজে লাগে।
</I18nBangla>

```sh
bnl
> print("hi");
hi
> 1 + 2
3
```

---

## Passing Arguments

<I18nEnglish>
Anything after the script path (or after the `-e` code) is forwarded to the script. Read them with `sys.arg(i)` / `sys.argc()`.
</I18nEnglish>

<I18nBangla>
স্ক্রিপ্ট পাথের পরে (বা `-e` কোডের পরে) যা কিছু থাকে, তা স্ক্রিপ্টের কাছে ফরওয়ার্ড হয়। `sys.arg(i)` / `sys.argc()` দিয়ে পড়ুন।
</I18nBangla>

<Tabs defaultValue="english" className="w-full">
  <TabsList>
    <TabsTrigger value="english">English</TabsTrigger>
    <TabsTrigger value="bangla">Bangla</TabsTrigger>
  </TabsList>

  <TabsContent value="english">

```bnl
// file: greet.bnl
import "sys" as sys;

var name = "Guest";
if (sys.argc() > 0) {
    name = sys.arg(0);
}
print("Hello,", name);
```

```sh
bnl greet.bnl Alice
# Output: Hello, Alice
```
  </TabsContent>

  <TabsContent value="bangla">

```bnl
// ফাইল: greet.bnl
আমদানি "sys" যেমন sys;

চলক নাম = "অতিথি";
যদি (sys.argc() > 0) {
    নাম = sys.arg(0);
}
লিখুন("হ্যালো,", নাম);
```

```sh
bnl greet.bnl আলিস
# আউটপুট: হ্যালো, আলিস
```
  </TabsContent>
</Tabs>

---

## Best Practices

<I18nEnglish>
- Keep scripts small and focused — one entry point per file.
- For anything past a handful of arguments, switch from `sys.arg` to the `cli` module for proper flag parsing.
- Use `bnl --version` in CI logs so you know which interpreter built which output.
</I18nEnglish>

<I18nBangla>
- স্ক্রিপ্ট ছোট এবং নির্দিষ্ট রাখুন — প্রতিটি ফাইলে একটি এন্ট্রি পয়েন্ট।
- কয়েকটির বেশি আর্গুমেন্ট হলে, প্রপার ফ্ল্যাগ পার্সিং-এর জন্য `sys.arg` থেকে `cli` মডিউলে শিফট করুন।
- CI লগে `bnl --version` ব্যবহার করুন, যাতে জানা যায় কোন ইন্টারপ্রেটার কোন আউটপুট তৈরি করেছে।
</I18nBangla>


